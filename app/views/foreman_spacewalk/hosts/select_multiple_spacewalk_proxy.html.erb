<%= render 'hosts/selected_hosts', :hosts => @hosts %>

<%= form_for :proxy, :url => update_multiple_spacewalk_proxy_hosts_path(:host_ids => params[:host_ids]) do |f| %>
  <%= multiple_proxy_select(f, 'Spacewalk') %>
<% end %>

